[build]
  functions = "netlify/functions"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# Optional: clean, valid redirect for SPA routing (keeps React working on refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
